import React from 'react';
import {StyleButton} from '../material-ui/StyleButton';
const ButtonCustomer = ({textButton}) => {
    return (
        <StyleButton>{textButton}</StyleButton>
    );
};

export {ButtonCustomer};